apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rabbitmq-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/lucasGalo/lab-rabbitmq.git'
    targetRevision: main
    path: k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
